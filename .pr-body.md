## Problem
This PR implements the Spatial Media Player feature #unknown (user requested feature), allowing users to spawn a YouTube iframe that is synchronized over CRDT.

## Changes
| File | Description |
|------|-------------|
| `shared/yjs-schema.ts` | Added `MediaPlayerState` to Yjs schema. |
| `client/context/SpaceContext.tsx` | Wired up Yjs mutation and observation for media player state across users. |
| `client/components/Canvas/MediaPlayer.tsx` | New component rendering drag/resizable YouTube component with spatial audio syncing |
| `client/components/Canvas/Canvas.tsx` | Renders `MediaPlayer` components on canvas from CRDT active state. |
| `client/components/Controls/ControlBar.tsx` | Added spawn button for the Media Player. |
| `e2e/dsl/*` | Added E2E testing DSL actions, properties, and views for the Media Player. |
| `e2e/scenarios/media-player.spec.ts` | End to end verification test for syncing a video player, moving it, resizing it, and terminating it. |

## Testing
- Automated: `just e2e-quick "media-player"` passed successfully.
- Ran full `just e2e-quick` regression suite which passed successfully.
